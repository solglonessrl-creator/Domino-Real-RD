{
  "name": "domino-real-rd",
  "version": "1.0.0",
  "description": "Domino Real RD - El dominó dominicano del mundo",
  "scripts": {
    "start": "cd backend && node server.js",
    "install-backend": "cd backend && npm install",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
