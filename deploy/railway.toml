# Domino Real RD — Deploy en Railway
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "domino-real-rd-backend"
source = "backend"
